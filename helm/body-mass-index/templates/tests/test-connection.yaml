apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "body-mass-index.fullname" . }}-test-connection"
  labels:
{{ include "body-mass-index.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "body-mass-index.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
